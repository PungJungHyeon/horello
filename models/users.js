class users{
    constructor(uid, uemail, upassword, uname, usex){
        this.uid = uid;
        this.uemail = uemail;
        this.upassword = upassword;
        this.uname = uname;
        this.usex = usex;
    }
}